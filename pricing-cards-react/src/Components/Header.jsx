export default function Header(){

    return(
        <header>
            <h1>Ship full-stack projects, database included.</h1>
            <p className="description">Git push to deploy. 
                Every project gets a managed Postgres instance, 
                edge CDN, and preview environments on every branch.</p>
        </header>
    )

}