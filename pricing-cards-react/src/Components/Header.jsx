export default function Header(){

    return(
        <header>
            <h1>Ship full-stack projects,<br />database included</h1>
            <p className="description">Git push to deploy.<br />
                Every project gets a managed Postgres instance, 
                edge CDN, and preview environments on every branch.</p>
        </header>
    )

}